version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: src/Kumashaurma.API/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: "Host=host.docker.internal;Port=5433;Database=kumashaurma_dev;Username=devuser;Password=dev123"
    ports:
      - "5000:80"
      - "5001:443"
    restart: unless-stopped

# PostgreSQL уже запущен отдельно, поэтому здесь только API
